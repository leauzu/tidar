# Deploy notes

- Source framework: Astro
- Static output: `dist/`
- Clean routes are generated as directories, for example:
  - `/profil`
  - `/visi-misi`
  - `/tugas-fungsi`
  - `/makna-lambang`
  - `/dokumen-resmi`
- Vercel can deploy from the project root using `npm run build`.
