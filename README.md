# TIDAR Astro Deploy

Astro static website with clean routes such as `/profil`, `/visi-misi`, and `/makna-lambang`.

## Commands

```bash
npm install
npm run dev
npm run build
```

The production build is generated in `dist/`.
